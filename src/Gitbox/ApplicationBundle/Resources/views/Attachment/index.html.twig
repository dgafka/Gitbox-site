{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Attachment list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Status</th>
                <th>Filename</th>
                <th>Title</th>
                <th>Description</th>
                <th>Createdate</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('attachment_show', { 'id': entity.id }) }}">{{ entity.status }}</a></td>
                <td>{{ entity.filename }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.createDate %}{{ entity.createDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('attachment_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('attachment_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('attachment_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
