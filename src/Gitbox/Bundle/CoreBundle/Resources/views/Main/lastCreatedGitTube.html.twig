<div class="row text-center">

    {% for result in results %}
        <div class="col-lg-3 col-md-6 hero-feature">
            <div class="thumbnail">
                <img class="main-movie-show" src="{{ asset('images/movie.png') }}" alt="">
                <div class="caption">
                    <h3 class="main-title-show">{{ result.title }}</h3>
                    <p class="main-description-show">{{ result.description }}</p>
                    <p>
                        <a href="{{ path('tube_content_show', {'login': result.login, 'id': result.id }) }}" class="btn btn-primary">Oglądaj...</a>
                        <span class="main-login-show"><a href="{{ path('user_profile_index', {'login' : result.login }) }}">{{ result.login }}</a></span>
                    </p>
                </div>
            </div>
        </div>
    {% endfor %}

</div>