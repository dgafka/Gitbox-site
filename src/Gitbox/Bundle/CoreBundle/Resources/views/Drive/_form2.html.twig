{% block newDrive_form %}
    <div class="register-form">
        <h2 style="margin-bottom: 20px;">{{ tytul }}</h2>
        {{ form_start(form) }}
            {{ form_errors(form) }}
            <div class="form-group">
                {{ form_label(form.title) }}
                {{ form_widget(form.title) }}
                {{ form_label(form.description) }}
                {{ form_widget(form.description) }}
                {{ form_widget(form.save, {
                'label': btnLabel
                }) }}
            </div>

        {{ form_end(form) }}

    </div>
{% endblock %}