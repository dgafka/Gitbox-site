{% extends "GitboxCoreBundle:Drive:DriveShow.html.twig" %}
{% block titlepanel %}
    <a href="{{ path('drive_show_menu', {'login': user.login, 'element': parentMenu}) }}">
        <span class="glyphicon  glyphicon-circle-arrow-left"></span>
    </a>
    <h2>
        {{ pageContent.title }}
    </h2>
    <a type="button"  href="{{ path('drive_contener_edit', {'login': user.login, 'element': pageContent.id }) }}">
        <span class="glyphicon glyphicon-cog"></span>
    </a>

{% endblock %}
{% block descpanel %}
{%  endblock %}
{% block tabelpanel %}
    {% include "GitboxCoreBundle:Drive:_menuContent.html.twig" %}
{% endblock %}
    {%  block buttpanel %}
    {% if logged == true %}
        <div class="driveButtonPanel">
            <a type="button" class="btn btn-default" href="{{ path('drive_contener_new', {'login': user.login, 'element': pageContent.id }) }}">
                Dodaj kontener
            </a>
            <a type="button" class="btn btn-default" href="{{ path('drive_item_new', {'login': user.login, 'element': pageContent.id }) }}">
                Dodaj element
            </a>
        </div>


    {%  endif %}
    {% endblock %}
